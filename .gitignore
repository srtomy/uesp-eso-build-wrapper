# deps
node_modules/

# build output
dist/

# env — never commit real secrets
.env
.env.local
.env.*.local

# debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript incremental cache
tsconfig.tsbuildinfo
.ts-out/

.idea

# test coverage output
coverage/

# Claude Code local config (tool-specific, not project state)
.claude/

# opencode local config (tool-specific, not project state)
.opencode/

# personal Claude Code instructions (kept local, holds user preferences)
CLAUDE.local.md

# Claude Code / opencode — symlink pro md-vault pessoal (path absoluto, não portável)
CLAUDE.md
AGENTS.md

# docs internas — symlinks pro md-vault privado, não vão pro repo público
docs/reference.md
docs/research

# local SQLite DB seeded from UESP dumps (regenerated via scripts/generate-data.ts)
local.db
*.db
